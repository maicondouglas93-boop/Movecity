// Política geográfica única compartilhada por despacho, agendamentos e mapa público.
// Alterar este valor exige decisão explícita de produto e testes de alcance.
module.exports = Object.freeze({
    CAPTAIN_SEARCH_RADIUS_KM: 15,
});
