VITE_APP_ROLE=driver
# Push FCM nativo no APK — obrigatório em release (CI valida).
# Desative só localmente com VITE_NATIVE_PUSH_ENABLED=false em .env.driver.local
# se ainda não tiver google-services.json.
VITE_NATIVE_PUSH_ENABLED=true
